% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonized_meta.R
\docType{data}
\name{harmonized_meta}
\alias{harmonized_meta}
\title{Harmonized Sample Metadata (cMD4)}
\format{
A \code{data.frame} with one row per sample and one column per
harmonized field. Built from
\code{inst/extdata/harmonized_meta.csv}, the staged copy of
\code{curatedMetagenomicDataCuration::makeCombinedMetadata()}.
}
\usage{
harmonized_meta
}
\description{
The \code{harmonized_meta} \code{data.frame} is the schema-aligned harmonized
sample metadata release for curatedMetagenomicData 4.0. It contains
one row per sample, restricted to the columns defined in the cMD
harmonization data dictionary, with curated clinical, lifestyle, and
sequencing fields and ontology term identifiers for values drawn from
controlled vocabularies. Use \link{all_meta} when you also need study-
specific columns that have not yet been folded into the schema.
}
\details{
Key columns used by the cMD4 data access layer are \code{study_name} and
\code{sample_id}; \code{sample_id} is the join key against the cMD4 DuckDB views
(it corresponds to the \code{sample_name} column in the ETL \code{sample_id_map}).

Users filter this table to the samples of interest and then pass the
subset to \code{\link[=returnSamples]{returnSamples()}} to obtain a \code{SummarizedExperiment} for the
desired data type, or use \code{\link[=curatedMetagenomicData]{curatedMetagenomicData()}} to retrieve
study-level resources by regular-expression pattern.
}
\seealso{
\link{all_meta}, \code{\link[=returnSamples]{returnSamples()}}, \code{\link[=curatedMetagenomicData]{curatedMetagenomicData()}}
}
\keyword{datasets}
